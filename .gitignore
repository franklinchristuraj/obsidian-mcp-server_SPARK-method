# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Environment
.env
*.local

# IDE
.vscode/
.cursor/
.codex/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Project specific
server.log
*.pid
.env.production
config_local.yaml
config.yaml
.env
*.key
*.pem
*.crt

# Workspace keys (contain real secrets — never commit)
workspace_keys.json

# Token database (live SQLite — never commit)
tokens.db

# Tool-call observability database (live SQLite — never commit)
observability.db
observability.db-wal
observability.db-shm

# MCP Tasks extension store (live SQLite — never commit)
tasks.db
tasks.db-wal
tasks.db-shm

# Virtual environment
.venv/

# Diagnostic / temp files
DIAGNOSTIC.md

# Sensitive documentation
SETUP_STATUS.md
REMOTE_ACCESS.md
expose_obsidian_ports.sh
recreate_obsidian_with_ports.sh

# Backup files
*.bak
*.backup
*~

# Temporary files
tmp/
temp/

# Documentation that may contain sensitive info (already moved to docs/)
# Keep these patterns for any future files
*_STATUS.md
*_ACCESS.md
*_PASSWORDS.md
*_SECRETS.md

# Local MCP dev server (scripts/dev-server.sh)
.dev-server.pid
dev-server.log
.claude/settings.local.json
.workspace_keys_issued.txt
